<% layout('layouts/boilerplate')%>
    <link rel="stylesheet" href="/stylesheets/app.css">

    <h1>Hostels</h1>
    <% for (let lodge of lodges) { %>
        <div class="container">
            <div class="card mb-3">
                <div class="row">
                    <div class="col-md-4">
                        <img src="<%= lodge.image %> " class="img-fluid index-img" alt="<%= lodge.name %> ">
                    </div>
                    <div class="col-md-8">
                        <div class="card-body">
                            <h5 class="card-title">
                                <%= lodge.name %>
                            </h5>
                            <p class="card-text">
                                <%= lodge.description %>
                            </p>
                            <p class="card-text">
                                <small class="text-muted text-capitalize">
                                    <%= lodge.location%>
                                </small>
                            </p>
                            <a class="btn btn-primary" href="/lodges/<%=lodge._id%>">View
                                <%=lodge.name%>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <% } %>